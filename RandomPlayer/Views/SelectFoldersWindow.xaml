<Window x:Class="RandomPlayer.Views.SelectFoldersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:RandomPlayer.ViewModels"
        xmlns:local="clr-namespace:RandomPlayer.Views"
        mc:Ignorable="d"
        Title="SelectFoldersWindow" Height="450" Width="800">

    <Window.Resources>
        <vm:SelectFoldersWindowViewModel x:Key="ViewModel"/>
    </Window.Resources>

    <Grid DataContext="{StaticResource ViewModel}" Background="{DynamicResource Color.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" ItemsSource="{Binding SelectedFolders}" Margin="10,10,10,0">
            
        </DataGrid>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Command="{Binding AddFolderCommand}" Style="{StaticResource Button1}">Ajouter</Button>
            <Button Grid.Column="2" Command="{Binding FinishCommand}" Style="{StaticResource Button1}">Terminer</Button>
        </Grid>
    </Grid>
</Window>
